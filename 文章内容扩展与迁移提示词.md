# 文章内容扩展与迁移任务提示词（Myblog 专用）

任务说明：对指定路径下的 Markdown 文件进行内容扩展、排版优化，并迁移至目标项目目录，同时确保输出内容符合技术文档规范与站点框架要求（VitePress）。

## 内容逻辑链

先总结全文，然后提供背景，根据核心观点/思想阐释，提供作用、意义、方向等延伸，最后落脚在当今的现实意义。

## 参考文件排版格式

- 排版规范：`F:\8月重新考研\个人博客\log\VitePress 目标目录与格式参考.md`

- 短文档格式：`F:\8月重新考研\个人博客\log\Bash语法.md`

## 待处理文件路径

`F:\8月重新考研\思考`

检查该路径下标题名前缀与今日日期相同的“月份日期-文章标题”的文章文件。

## 输出目标根目录

`E:\E_projects\myblog\`

***

## 一、文件处理方式

1. 不得修改或覆盖日志目录下的原文件；在原文件所在目录不变的前提下，创建副本并粘贴至目标项目对应子文件夹内。

2. 若目标子文件夹不存在，则新建该子文件夹。

3. 幂等性校验：目标子文件夹中若已存在同名已迁移文件（已含 frontmatter 与 VitePress 排版），视为已完成迁移，跳过并记录，不重复改写、不覆盖。

## 二、内容扩展与改写要求

1. 对原文进行适度扩展，使表述更专业、准确、流畅。

2. 保持原意核心逻辑与技术事实不变，不可引入与原文相悖的信息。

3. 表述以客观、陈述性为主，避免主观评价；禁止使用比喻、排比等修辞。

4. 扩写围绕技术细节、使用场景、常见问题或逻辑补充展开，增强实用性与完整性。

## 三、排版与格式要求

1. 输出必须符合 VitePress 默认 Markdown 扩展语法：Frontmatter、代码块高亮、容器（Tip/Warning/Note 等）、表格、内部链接。

2. 标题层级清晰，合理使用一至四级标题、有序/无序列表、代码块、提示块等元素；文档仅保留一个一级标题。

3. 目标为降低阅读与使用成本，提升信息检索效率与可读性。

## 四、内容分类规则（依据 config.mts 的真实栏目）

按主要性质归入以下目录之一：

| 目录         | 栏目      | 内容定位                        |
| ---------- | ------- | --------------------------- |
| `Tech/`    | 技术笔记    | 编程、框架、DevOps、技术原理、工具实践      |
| `Product/` | 产品分析    | 产品拆解、用户体验、商业逻辑、功能分析         |
| `ACGN/`    | ACGN 评价 | 动画、漫画、游戏、轻小说及相关文化评论         |
| `Project/` | 项目复盘    | 项目复盘、流程规范、工程记录              |
| `Growth/`  | 成长思考    | 个人成长、职场观察、学习方法、职业规划、读书/视频感悟 |
| `English/` | 英语学习    | 英语学习、雅思备考、听说读写训练            |
| `Me/`      | 关于我     | 个人介绍、职业背景、联系方式              |
| `news/`    | 资讯观察    | AI、科技、行业短讯，不在主导航展示          |

分类优先级：`Tech` > `Product` > `ACGN` > `Project` > `Growth` > `English` > `news` > `Me`（兜底归入 `Project/`）。

## 五、Frontmatter 规范

每篇文章开头使用 YAML frontmatter：

```yaml
---
title: 20260823-内容标题       # 规则：发布日期前缀 + 内容标题，如 20260825-人生愿景的魔力
description: 一句话摘要，用于列表展示
date: 2026-08-23               # 取源文件名前缀日期
category: Tech                 # 与目标目录一一对应（Growth/English 等）
tags:
  - 标签一
  - 标签二
---
```

`title` 与 `category` 需与目标目录匹配；`title` 前缀日期与 `date` 字段应一致。

## 六、执行步骤（按优先级顺序）

1. 内容理解与分类判断：读取文件内容，判断主题类别，确定目标子文件夹（对照第四节映射表）。

2. 幂等性检查：确认目标任务是否已迁移，命中则跳过。

3. 排版标准化与 VitePress 适配：按参考格式调整结构，补齐 frontmatter，确保语法兼容。

4. 文本扩写与润色：在保持客观前提下扩展技术细节、场景说明或补充信息。

5. 文件复制与迁移：将改写后的副本写入目标子文件夹，文件名沿用原文件名（不得更改）。

## 七、最终交付物

- 分类后的 Markdown 文件副本，存放于对应子文件夹中，文件名沿用原文件名。

- 汇总本次新增/迁移的文件清单及跳过的文件及原因。

## 八、版本同步

完成上述操作后，将整个 `E:\E_projects\myblog` 目录下的变更推送至 GitHub 远程仓库。

1. 确认本地仓库已初始化且关联远程地址（`origin=`<https://github.com/StaminaMyGo/myblog.git``）；若未关联则先执行关联。>

2. 仅 add 本次涉及的新增文件，避免误提交无关内容。

3. 推送分支默认为当前分支（`main`）。

