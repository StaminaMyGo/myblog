# 文章迁移任务提示词（Myblog v2）

任务说明：将 `F:\8月重新考研\思考` 下指定日期的 Markdown 文章，扩写、排版并迁移到 VitePress 博客仓库 `E:\E_projects\myblog`。**每次迁移都必须通过校验与构建验证，确保新增文件不会导致渲染页（尤其标签页）崩溃。**

本文件为 v2 版，取代旧的 `文章内容扩展与迁移提示词.md`。

## 一、路径与目录

| 项 | 路径 |
| --- | --- |
| 源目录 | `F:\8月重新考研\思考`（内部分类为 `2026-8` / `2026-9` / `2026-10` 等月目录） |
| 源文件筛选 | 月目录下文件名为 `MMDD-标题.md` 者，取前缀与本次目标日期相同（如 `0909-...`）的文件 |
| 输出根目录 | `E:\E_projects\myblog` |
| 格式参考 | 仓库内已迁移且通过的成熟文章，优先看 `English\0905-美国人口语中最常用的100个习语.md`（frontmatter + 表格 + 提示块 + 单一 `#` 一级标题） |

## 二、分类规则（与 `.vitepress/config.mts` 的 `CATEGORIES` 一致）

按主要性质归入对应目录，`category` 字段与目录名一一对应：

| 目录 | category | 内容定位 |
| --- | --- | --- |
| `Tech/` | Tech | 技术笔记：编程、框架、DevOps、技术原理、工具实践 |
| `Product/` | Product | 产品分析：产品拆解、用户体验、商业逻辑、功能分析 |
| `ACGN/` | ACGN | ACGN 评价：动画、漫画、游戏、轻小说及相关文化评论 |
| `Project/` | Project | 项目复盘：项目复盘、流程规范、工程记录 |
| `Growth/` | Growth | 成长思考：个人成长、职场观察、学习方法、职业规划、读书/视频感悟 |
| `English/` | English | 英语学习：英语学习、雅思备考、听说读写训练 |
| `news/` | news | 资讯观察：AI、科技、行业短讯（不在主导航展示） |
| `Me/` | Me | 关于我：个人介绍、职业背景、联系方式 |

分类优先级：`Tech` > `Product` > `ACGN` > `Project` > `Growth` > `English` > `news` > `Me`（兜底归入 `Project/`）。

## 三、Frontmatter 规范

每篇开头使用 YAML frontmatter：

```yaml
---
title: 0909-内容标题
description: 一句话摘要，用于列表与搜索展示
date: 2026-09-09
category: Tech
tags:
  - 标签一
  - 标签二
---
```

要求：

1. `title` 前缀日期即文件名前缀（`0909-`），`date` 为对应的 `YYYY-MM-DD`，两者日期一致。
2. `category` 与目标目录名一致。
3. `tags` **每个值都必须加引号**，尤其不要出现裸数字。YAML 会把 `- 408` 解析成 `number`，`TagsPage` 排序时 `localeCompare` 对 non-string 会崩溃，这是**最可能导致渲染页崩溃的根因**。正确写法：`- "408"`。
4. `description` 必填，作为列表/搜索摘要。

## 四、内容扩写与排版要求

1. 对原文适度扩写，更专业、准确、流畅；保持原意核心逻辑与技术事实不变，不引入相悖信息。
2. 表述客观、陈述性为主，避免主观评价与修辞（比喻、排比）。
3. 扩写围绕技术细节、使用场景、常见问题或逻辑补充展开。
4. 输出须符合 VitePress Markdown 语法：frontmatter、代码块高亮、容器（`::: tip` / `::: warning` / `::: note`）、表格、内部链接。
5. 标题层级清晰（一至四级），文档**仅保留一个一级标题** `# 标题`。
6. 目标：可读、信息检索效率高。

## 五、迁移流程（每次必须按序执行）

1. **读源文件并判断分类**：确定目标子目录（对照第二节）。
2. **幂等性检查**：目标子目录中若已存在同名、已含 frontmatter 与 VitePress 排版的迁移文件，视为已完成，跳过并记录，不重复改写、不覆盖。
3. **排版标准化**：按第三节补齐 frontmatter，按第四节调整结构，确保语法兼容。
4. **扩写润色**：在保持客观前提下扩展细节、场景或补充信息。
5. **写入目标子目录**：文件名沿用原文件名（不改名），不修改源目录原文件，仅在源目录创建副本内容后写入目标。

## 六、交付前验证（重点：防渲染崩溃）

迁移完成、提交之前，必须执行以下三项，**任何一个失败都不得提交**：

1. **规范校验**：在 `E:\E_projects\myblog` 下运行
   ```bash
   node scripts/check.mjs
   ```
   该脚本扫描 `Tech/Product/ACGN/Project/Growth/English/Me/news` 全部文章，检查：缺少 frontmatter / 缺少 title / 缺少 date / date 格式非 `YYYY-MM-DD` / 缺少 tags（警告）/ **tags 含未加引号的裸数字（报错，会导致标签页崩溃）** / 全局重复标题（警告）。出现 `❌ 错误` 必须修正后重跑，直到 `✅ 全部通过`。
   缺少 tags 只是警告，可接受但建议补全。
2. **构建验证**：运行 `npx vitepress build`，必须 `build complete`，无中断、无渲染异常。
3. **（可选）标签页运行验证**：`pnpm preview` 后用脚本 `node scripts/check_tags.mjs`（或手动打开 `http://localhost:4174/tags/`），确认标签云、标签筛选正常渲染，控制台无 `uncaught` 异常 / `TypeError`。

> 校验通过的标准：`check.mjs` 无错误 + `vitepress build` 成功。这两项即可保证新增文件不破坏渲染页；标签页的裸数字标签问题是历史崩溃点，务必靠第 1 步的报错拦截。

## 七、版本同步（每批迁移后执行，本地 + 远程备份）

1. 确认当前在 `E:\E_projects\myblog`，远程已关联：`origin` → `https://github.com/StaminaMyGo/myblog.git`。
2. **只 add 本次涉及的新增/修改文件**（新增分类下的 `.md`、本次改动的 `scripts/`、`.gitignore` 等），避免误提交无关内容、诊断文档、构建产物（`.vitepress/dist/`、`node_modules/` 已在 `.gitignore`）。
3. 提交信息格式建议：`migrate YYYY-MM-DD articles: <标题> (<Category>), <标题> (<Category>)`，与仓库历史风格一致。
4. 推送当前分支（`main`）：
   ```bash
   git add <本次文件>
   git commit -m "..."
   git push origin main
   ```
5. 推送是硬性要求：本流程以 GitHub 远程作为唯一可靠备份，**每次删改或提交后必须推送**，并确认推送成功（远程 commit 一致）。

## 八、清理与维护注意事项

- 迁移相关诊断/分析文档（如 `博客结构与发展分析.md`、`诊断报告_标签页与搜索框.md`）不属于站点内容，不参与渲染与构建；不要将它们加入分类目录或导航。
- 工具/会话缓存目录（`.omo/`、`.playwright-mcp/`、`.codegraph/`）已在 `.gitignore` 中，不提交。
- 不要在仓库根目录残留临时目录如 `-Force`（由 PowerShell 误用产生，空目录），此类目录应删除并加入 `.gitignore`。

## 九、交付物

- 分类后的 Markdown 文件副本，存于对应子目录，文件名沿用原名。
- 汇总清单：本次新增/迁移的文件、跳过的文件及原因、`check.mjs` 与 `build` 结果。
